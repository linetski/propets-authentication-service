spring.application.name = propets-auth-service
spring.cloud.config.uri=${CONFIG-SERVICE-URL:http://localhost:8888}
server.port = ${PORT}

logging.file.path=logs
logging.pattern.dateformat: yyyy-MM-dd HH:mm:ss.SSS,Asia/Jerusalem

spring.main.allow-bean-definition-overriding=true

spring.rabbitmq.host = rabbitmq3
spring.rabbitmq.username=myuser
spring.rabbitmq.password=mypassword
rabbitmq.exchange=email
rabbitmq.queue=email
rabbitmq.routingkey=email

management.endpoint.loggers.enabled=true

 

#Metrics related configurations

management.endpoint.logfile.enabled=true

management.endpoint.metrics.enabled=true

management.endpoints.web.exposure.include=*

management.endpoint.prometheus.enabled=true

management.metrics.export.prometheus.enabled=true

 

# for admin app

management.endpoint.health.show-details=always
